# Production Environment Variables for VPS
# Copy this file to your VPS as .env

# Database - Use absolute path for SQLite in production
DATABASE_URL="file:/var/www/otavio-edital/prisma/production.db"

# NextAuth.js - Update with your VPS domain
NEXTAUTH_URL="https://your-domain.com"
NEXTAUTH_SECRET="your-production-secret-key-here-generate-a-strong-one"

# Google OAuth - Use production credentials
GOOGLE_CLIENT_ID="your-production-google-client-id"
GOOGLE_CLIENT_SECRET="your-production-google-client-secret"

# GitHub OAuth - Use production credentials
GITHUB_ID="your-production-github-client-id"
GITHUB_SECRET="your-production-github-client-secret"

# Environment
NODE_ENV="production"

# Instructions:
# 1. Copy this file to your VPS as .env in the project root
# 2. Update all placeholder values with real production values
# 3. Ensure the database path exists and has proper permissions
# 4. Generate a strong NEXTAUTH_SECRET using: openssl rand -base64 32